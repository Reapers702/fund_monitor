# 基金监控与 AI 推荐系统（fund_monitor）· 计划书

> 版本：v0.3（评审稿，技术路线已定：**Electron**）
> 日期：2026-08-12
> 目标产物：一个 Electron 桌面应用，自动抓取自选基金（净值 / 盘中估值 / 重仓股 / 个股行情），新闻**不抓取**（由另一套 24h 运行的程序写入 `ai_fund` 库 `raw_news` 表，本应用只读），由 DeepSeek 基于"净值走势 + 重仓股 + 相关新闻 + 持仓成本"给出 加仓/减仓/持有 建议，并推送桌面通知。
> 路线演进：最初规划 Wails v2（复用 gold_monitor 的 Go 栈），经评审发现 Wails 的 WebView 是黑盒——Go 侧无法控制页面、拦截请求、拿 cookie，且前端跑在 `wails://` 源下有 CORS 限制（只能 JSONP 绕）。**改用 Electron**：主进程（Node）直接掌握 Chromium 控制面，具备 Playwright 级抓取能力，从架构上解决"抓取/抗反爬"问题。

---

## 一、项目背景与目标

把 `ai-fund-recommend` 的功能目标（基金净值/持仓/个股行情/新闻抓取 + LLM 分析）与 `gold_monitor` 的产品形态（桌面应用 + PostgreSQL + DeepSeek 建议 + 桌面通知 + 持仓管理）结合，用 **Electron** 实现一款基金监控与 AI 推荐桌面应用：

- **多基金自选列表**：按 6 位代码添加/停用任意场外基金，启动自动补全基本信息（名称、经理、成立日）。
- **净值追踪**：拉取历史净值全量，之后每日盘后自动增量更新当日净值（T 日晚上公布）。
- **盘中估值（必做）**：交易时段展示基金估算涨跌（跟踪型基金用跟踪指数/ETF 实时行情；主动型基金走隐藏窗口抓页面估值）。
- **重仓股跟踪**：定期抓取最新季度重仓股及权重；抓日线/实时行情，作为 AI 分析输入。
- **新闻情报（只读，不抓取）**：读取另一套 24h 程序已写入 `ai_fund` 库 `raw_news` 表的财联社新闻（含 LLM 摘要 + 情绪判定 + 标签），本应用仅消费。
- **AI 建议**：每个交易日收盘后调用 DeepSeek，输出 `加仓/减仓/持有`，非持有触发桌面通知。
- **持仓管理**：手动录入每笔买卖（份额/金额/费率），自动计算移动加权平均成本与收益。
- **多客户端安全**：与 gold_monitor 相同的唯一约束 + `ON CONFLICT` 幂等写入。

---

## 二、旧项目回顾结论

### 2.1 gold_monitor（Wails + Go）——产品形态蓝本，代码不能直接复用

| 资产 | 说明 | 在 Electron 中的去向 |
|---|---|---|
| 桌面应用形态 | 自动采集 + 走势可视化 + AI 建议 + 通知 + 持仓 | ✅ 产品形态照搬 |
| 表结构与 SQL | gold_price_* / ds_advice / trade / profile | ✅ **表结构直接移植**（换字段名） |
| analyzer prompt / 结果解析 | 固定 JSON 输出、response_raw 留痕 | ✅ 逻辑照搬为 TS |
| 持仓算法 | 移动加权平均 + 费率 | ✅ 逻辑照搬为 TS |
| 配置体系 | 应用目录 config.json + .env 回退 | ✅ 照搬 |
| 幂等写入 / 并发兜底 | UNIQUE + ON CONFLICT | ✅ 照搬 |
| **Go / Wails 代码本身** | app.go / main.go / internal/* | ❌ 不迁移（技术栈已换） |

### 2.2 ai-fund-recommend（Python + Playwright）——功能清单与数据源盘点

| 数据源 | 原实现 | 依赖浏览器的真实原因 | Electron 中的方案 |
|---|---|---|---|
| 蛋卷基金详情 | Playwright 页面内 fetch(djapi) | 仅 CORS/反爬绕过 | 主进程 Node 直接 GET（Node 无 CORS） |
| 天天基金持仓 | Playwright DOM 爬表格 | HTML 需要渲染 | 东财 `FundArchivesDatas`（已实测）或隐藏窗口读 DOM |
| 雪球个股行情 | Playwright 页面内 fetch | 真反爬：需会话 cookie | 主进程直接调东财 push2his（已实测）；雪球用隐藏窗口种 cookie 后带 cookie 请求 |
| 财联社新闻 | 纯 requests + MD5(SHA1()) 签名 | 无 | **不抓取**：另一套 24h 程序写入 `ai_fund.raw_news`，本应用只读 |

> 关键结论：原项目的 Playwright 90% 用于"页面内 fetch 绕 CORS + 拿会话 cookie"。**Electron 主进程（Node）天然没有 CORS**，Node 的 fetch/undici 可直接调 JSON API；需要"浏览器形态"时，主进程还能用 Chromium 的隐藏窗口/executeJavaScript/webRequest/CDP——这正是 Playwright 的能力，等于**把 Playwright 内置进应用**，不依赖任何外部工具。

---

## 三、核心问题回答：Electron 能否做到 Playwright 的效果？

**结论：能，且这是架构决定的——Electron 主进程（Node）直接掌握 Chromium 控制面，等效"内置 Playwright"的桌面壳。**

### 3.1 与 Wails 的能力对比（已查证 Wails v2.14 源码）

| 能力 | Wails v2.14 | Electron（主进程） |
|---|---|---|
| 在任意页面执行 JS 并取回结果 | ❌ 仅 `runtime.WindowExecJS` 在**自己的 SPA**（`wails://` 源）内执行 | ✅ `webContents.executeJavaScript()` |
| 加载第三方站点抓取 | ❌ webview 只渲染自家前端 | ✅ 隐藏 `BrowserWindow` 加载任意 URL |
| 拦截/嗅探网络请求 | ❌ | ✅ `session.webRequest` + CDP `Network.getResponseBody` |
| 读写 Cookie | ❌ 拿不到 webview cookie | ✅ `session.cookies` |
| CDP（Playwright 底层协议） | ❌ | ✅ `webContents.debugger` |
| 跨域请求 | ⚠️ SPA 有 CORS（只能 JSONP 绕） | ✅ **爬虫全在主进程跑，Node 无 CORS** |

### 3.2 抓取双通道（Electron 版）

| 通道 | 实现 | 适用 |
|---|---|---|
| **Node HTTP 通道**（默认） | 主进程 undici/fetch + UA/Referer + 退避 | 全部已实测 JSON API（lsjz / pingzhongdata / jjcc / push2his / push2 / 蛋卷 djapi / 财联社） |
| **浏览器形态通道**（pageFetcher） | 隐藏 BrowserWindow → `executeJavaScript` / `webRequest` 拦截 / CDP 取响应体 / `session.cookies` | 主动型基金页面估值（T2）、雪球（cookie）、以及未来任何强反爬源 |

- `config.fetch.channel`：`node`（默认）/ `browser` / `auto`（按源失败自动切换）。
- 对东财：Node 直连已全通（实测）；万一将来被反爬，切 browser 通道的 JSONP 或隐藏窗口抓取。

---

## 四、数据源选型与接口实测（已用 curl 验证，2026-08-12）

### 4.1 实测结果总表（基金代码 017193，个股 600519）

| # | 数据 | 接口 | 状态 | 备注 |
|---|---|---|---|---|
| 1 | 历史净值（全量） | `https://fund.eastmoney.com/pingzhongdata/{code}.js` | ✅ 200 | JS 文本：`fS_name`、`Data_netWorthTrend`（日净值+涨跌）、`Data_ACWorthTrend`、`stockCodes`。一次性全量，正则提取 |
| 2 | 历史净值（分页，主推） | `https://api.fund.eastmoney.com/f10/lsjz?fundCode={code}&pageIndex=1&pageSize=N` | ✅ 200 JSON | `FSRQ/DWJZ/LJJZ/JZZZL/SGZT/SHZT`；当日净值已能查到；支持 `callback=` JSONP。需 Referer |
| 3 | 基金详情 | `https://danjuanfunds.com/djapi/fund/{code}` | ✅ 200 JSON | `fd_name` `manager_name` `found_date` 等。需 Referer |
| 4 | 重仓股（季度） | `http://fundf10.eastmoney.com/FundArchivesDatas.aspx?type=jjcc&code={code}&topline=10` | ✅ 200 | `var apidata={content:"...<table>..."}`，HTML 字符串解析 |
| 5 | 个股日线 K | `https://push2his.eastmoney.com/api/qt/stock/kline/get?secid={市场}.{code}&klt=101&fqt=1&fields2=...&lmt=N` | ✅ 200 JSON | 全量 K 线，完全开放；支持 `cb=` JSONP。沪 `1.`/深 `0.` |
| 6 | 财联社电报 | `https://www.cls.cn/api/cache?...&sign={MD5(SHA1(sorted query))}` | ✅ 200 JSON | 签名已复现；实测返回 21 条 `title/brief/content/ctime/subjects` |
| 7 | 盘中估值（fundgz） | `https://fundgz.1234567.com.cn/js/{code}.js` | ❌ 404 | 彻底失效（带完整浏览器头仍 404），放弃 |
| 8 | 盘中估值（fundmobapi） | `FundMNewApi/FundMNFInfo` | ⚠️ 61136 | 带完整参数仍失败，不可靠 |
| 9 | 雪球行情 | `stock.xueqiu.com/v5/...` | ⚠️ 依赖 cookie | 作为估值/行情备选源，用 pageFetcher 种 cookie 后访问 |
| E | 实时行情（push2） | `push2.eastmoney.com/api/qt/stock/get?secid=1.000300&fields=f43..f170` | ✅ 200 JSON | 沪深300 实时（价格×100，`f170`=涨跌幅）——**指数/ETF 盘中估算的数据基础** |

### 4.2 数据源组合策略（按可靠性排序）

1. **净值/涨跌/申赎**：东财 f10 `lsjz`（JSON，主）＋ `pingzhongdata`（全量补种，辅）。
2. **基金详情**：蛋卷 `djapi`（JSON）；失败降级 `pingzhongdata` 的 `fS_name`。
3. **重仓股**：`FundArchivesDatas?type=jjcc`（固定表格结构，解析成本低）。
4. **个股行情**：`push2`（盘中实时）＋ `push2his`（日 K，收盘后增量）。
5. **新闻**：不抓取——读 `ai_fund` 库 `raw_news` 表（另一套 24h 程序采集 + LLM 增强，本应用只读）。
6. **兜底**：任何源被反爬 → `pageFetcher` 浏览器形态通道。

### 4.3 抓取频率与风控策略（个人低频原则）

> 这些接口是**网页/App 自用的半公开接口**，非官方开放 API，"风控风险"客观存在。但本工具是**单用户桌面应用**（自己的 IP、低频请求），风险主要靠"频率纪律 + 请求形态 + 容错降级"管理。

| 数据源 | 设计频率 | 风控形态 | 对策 |
|---|---|---|---|
| 东财 `lsjz`（净值） | 全量仅首启 1 次；之后每日盘后 10min 轮询至当日净值出现即停 | 页面级接口，频率天然低 | 低频 + 缓存 |
| `pingzhongdata` | 仅首启/手动刷新 | 同上 | 一次性 |
| `jjcc`（季度持仓） | 每周 1 次 | 数据季度内固定 | 低频 |
| `push2/push2his`（行情） | 每基金持仓 10 只；实时 30s / 日线每日 1 次 | 行情基础设施，本就高频设计，偶发限流码 | 缓存 + 遇限流退避 |
| 蛋卷 `djapi` | 首启/手动刷新 | 移动 App 接口，偶发设备校验 | Referer/UA + 失败降级东财 |
| 财联社（新闻） | 不抓取（只读 `ai_fund.raw_news`） | 无 | 采集由另一套 24h 程序负责，本应用仅读 |

**通用抓取礼仪（编码约束）**：

1. **缓存优先**：能复用数据库已有数据就不重拉（沿用 ai-fund-recommend 的 TTL 缓存思路）。
2. **请求伪装**：统一 Chrome 桌面 UA + 对应站点 Referer + `Accept-Language: zh-CN`。
3. **退避与容错**：非 200 / 业务错误码（东财 61136 等）→ 指数退避（30s→1m→5m）重试有限次，绝不轰炸；连续失败记录日志。
4. **限流自检**：分析前批量取数并发上限 ≤3。
5. **数据源隔离**：每个源独立模块，故障自动/手动切换（蛋卷 ↔ 东财互为备份；Node ↔ browser 双通道）。新闻为只读，不参与抓取。
6. **浏览器形态兜底**：强反爬源走 pageFetcher（隐藏窗口 + cookie + CDP）。

> 结论：风控风险可控。个人低频使用下，最大的实际风险不是封禁而是**接口改版失效**（`fundgz` 已 404 即例证）——设计重点是"单点隔离、换源快、降级稳"。

### 4.4 盘中估值数据源策略（必做，分层实现）

| 层 | 适用 | 数据源 | 状态 |
|---|---|---|---|
| **T1 跟踪型估算** | 指数基金 / ETF 联接 | 东财 push2 实时行情取跟踪指数/ETF 涨跌幅（`f170`） | ✅ 已实测（沪深300） |
| **T2 页面估值** | 主动型基金 | pageFetcher 隐藏窗口加载 `fund.eastmoney.com/{code}.html`（或雪球 F 页面），读估值 DOM 或嗅探页面估值接口 | ⏳ M8 实施时验证 |
| **T3 降级** | 所有 | 盘中仅展示重仓股实时行情，基金等盘后净值 | 兜底 |

- **跟踪指数如何确定**：基金简称关键词匹配（"沪深300 / 中证XX / 上证XX / 恒生…"）→ 指数代码；匹配不到落 T2/T3。

---

## 五、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 桌面框架 | **Electron**（Chromium + Node 主进程） | 主进程即爬虫/业务层，渲染进程即 UI |
| 前端 | Vue 3 + TypeScript + ECharts + Naive UI | 走势图 ECharts，UI 组件 Naive UI |
| 构建 | **electron-vite**（main / preload / renderer 三端）+ electron-builder 打包 | 开发热更新，打包 NSIS |
| 包管理/工具 | **npm**（随 Node 提供，11.6.2 已装）：`npm install` / `npm run`；单测用 vitest | 依赖全部装在项目内 `node_modules`，**零全局安装** |
| 数据库 | PostgreSQL（`pg` node-postgres） | 用户配置 DSN，启动自动建表 |
| AI | DeepSeek API（OpenAI 兼容），主进程 fetch 直调 | 用户配置 Key/baseUrl/model |
| 通知 | Electron `Notification`（主进程） | Windows toast，无需额外初始化 |
| 爬虫 | 主进程 undici/fetch（Node 无 CORS）+ pageFetcher（隐藏 BrowserWindow + executeJavaScript / webRequest / CDP / cookies） | **无 Playwright 依赖** |
| 调度 | Node timers + node-cron（cron 式分钟触发） | 轻量 |
| 测试 | vitest（单测）+ Playwright（可选 e2e，驱动 Electron 调试，非运行时依赖） | |

> 环境确认：Node.js **v24.12.0** + npm **11.6.2**（已装，满足 ≥20 LTS）；Electron 二进制由 electron-builder 首次安装时下载（需网络）。所有依赖装进项目 `node_modules`，**无需任何全局安装**。主进程代码按 Node 兼容编写。

---

## 六、数据库设计

用户自配 PostgreSQL，程序启动自动建表（`CREATE TABLE IF NOT EXISTS`），结构与 gold_monitor 同思路。

### 6.1 `fund_basic`（基金基础信息）

```sql
CREATE TABLE IF NOT EXISTS fund_basic (
    fund_code   VARCHAR(8)  PRIMARY KEY,
    fund_name   TEXT        NOT NULL,
    fund_full_name TEXT,
    manager     TEXT,
    keeper      TEXT,
    found_date  DATE,
    is_active   INT         NOT NULL DEFAULT 1,
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

### 6.2 `fund_nav_daily`（每日净值，核心表）

```sql
CREATE TABLE IF NOT EXISTS fund_nav_daily (
    id          BIGSERIAL PRIMARY KEY,
    fund_code   VARCHAR(8)  NOT NULL,
    trade_date  DATE        NOT NULL,
    dwjz        NUMERIC(10,4),
    ljjz        NUMERIC(10,4),
    jzzzl       NUMERIC(8,4),
    sgzt        VARCHAR(16),
    shzt        VARCHAR(16),
    created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
    CONSTRAINT uq_nav UNIQUE (fund_code, trade_date)
);
CREATE INDEX IF NOT EXISTS idx_nav ON fund_nav_daily (fund_code, trade_date DESC);
```

- 历史补种 `ON CONFLICT DO NOTHING`；当日净值滚动更新 `DO UPDATE`。

### 6.3 `fund_estimate`（盘中估值，新增）

```sql
CREATE TABLE IF NOT EXISTS fund_estimate (
    id         BIGSERIAL PRIMARY KEY,
    fund_code  VARCHAR(8)  NOT NULL,
    est_time   TIMESTAMPTZ NOT NULL,              -- 估值时刻
    est_nav    NUMERIC(10,4),
    est_pct    NUMERIC(8,4),                      -- 估算涨跌幅 %
    source     VARCHAR(16),                       -- tracking_index / page / none
    CONSTRAINT uq_est UNIQUE (fund_code, est_time)
);
CREATE INDEX IF NOT EXISTS idx_est ON fund_estimate (fund_code, est_time DESC);
```

- 盘中按 `est_time` 幂等；当日收盘后清理由前端按时间窗展示。

### 6.4 `fund_holdings`（季度重仓股）

```sql
CREATE TABLE IF NOT EXISTS fund_holdings (
    id           BIGSERIAL PRIMARY KEY,
    fund_code    VARCHAR(8) NOT NULL,
    report_date  DATE       NOT NULL,
    rank         INT        NOT NULL,
    stock_code   VARCHAR(16),
    stock_name   TEXT,
    weight       NUMERIC(8,4),
    CONSTRAINT uq_hold UNIQUE (fund_code, report_date, rank)
);
```

### 6.5 `stock_daily`（个股日线行情）

```sql
CREATE TABLE IF NOT EXISTS stock_daily (
    id          BIGSERIAL PRIMARY KEY,
    stock_code  VARCHAR(16) NOT NULL,
    trade_date  DATE        NOT NULL,
    close       NUMERIC(10,2),
    open        NUMERIC(10,2),
    high        NUMERIC(10,2),
    low         NUMERIC(10,2),
    pct         NUMERIC(8,4),
    volume      NUMERIC(20,0),
    amount      NUMERIC(24,2),
    CONSTRAINT uq_stock UNIQUE (stock_code, trade_date)
);
CREATE INDEX IF NOT EXISTS idx_stock ON stock_daily (stock_code, trade_date DESC);
```

### 6.6 新闻表（不建，只读 `ai_fund` 库）

> **范围调整（2026-08-13）**：本应用**不抓取新闻**。财联社新闻由另一套 24h 运行的程序采集并 LLM 增强，写入同一 PostgreSQL 实例的 `ai_fund` 库 `raw_news` 表。本应用通过 `config.aiFund` 只读连接消费，不在 `fund_monitor` 库建新闻表。供参考的 `ai_fund.raw_news` 结构（已实测 43,258 行）：

```sql
-- ai_fund.raw_news（另一程序维护，本应用只读）
CREATE TABLE IF NOT EXISTS raw_news (
    id         TEXT        PRIMARY KEY,
    title      TEXT,
    content    TEXT,
    pub_time   TIMESTAMP,
    source     TEXT,
    tags       JSONB,      -- 官方题材标签
    processed  INTEGER DEFAULT 0,
    summary    TEXT,       -- LLM 摘要
    sentiment  TEXT,       -- LLM 情绪
    llm_tags   JSONB       -- LLM 主题标签
);
```

### 6.7 `ds_advice`（AI 建议留痕）

```sql
CREATE TABLE IF NOT EXISTS ds_advice (
    id            BIGSERIAL PRIMARY KEY,
    fund_code     VARCHAR(8) NOT NULL,
    trade_date    DATE       NOT NULL,
    action        VARCHAR(8) NOT NULL,            -- add / reduce / hold
    reason        TEXT,
    confidence    NUMERIC(4,2),
    response_raw  JSONB,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS idx_advice ON ds_advice (fund_code, trade_date DESC);
```

### 6.8 `fund_trade` + `fund_profile`（持仓）

```sql
CREATE TABLE IF NOT EXISTS fund_trade (
    id         BIGSERIAL PRIMARY KEY,
    fund_code  VARCHAR(8)  NOT NULL,
    trade_type VARCHAR(4)  NOT NULL,              -- buy / sell
    shares     NUMERIC(14,2) NOT NULL,
    price      NUMERIC(10,4) NOT NULL,
    fee        NUMERIC(10,4) NOT NULL DEFAULT 0,
    trade_date TIMESTAMPTZ NOT NULL,
    note       TEXT
);
CREATE INDEX IF NOT EXISTS idx_ftrade ON fund_trade (fund_code, trade_date);

CREATE TABLE IF NOT EXISTS fund_profile (
    id        INT PRIMARY KEY DEFAULT 1,
    buy_fee_pct  NUMERIC(6,4) NOT NULL DEFAULT 0,
    sell_fee_pct NUMERIC(6,4) NOT NULL DEFAULT 0
);
```

- 持仓计算：按 `fund_code` 时间序移动加权平均，买入更新平均成本，卖出按 `(净值−成本)×份额` 计已实现盈亏；成本与盈亏均已扣费率。
- AI 分析时按基金携带持仓信息。

---

## 七、系统架构与模块划分

```
┌────────────────────────────────────────────────────────────────┐
│                    Electron 桌面应用 (fund_monitor)             │
│                                                                │
│  ┌──────────────── 主进程 (Node，无 CORS) ────────────────┐    │
│  │  crawler    news     analyzer    position    storage    │    │
│  │  ┌───────────────────────────────────────────────────┐  │    │
│  │  │  pageFetcher（浏览器形态通道，Playwright 级）        │  │    │
│  │  │  隐藏 BrowserWindow + executeJavaScript            │  │    │
│  │  │  + webRequest 拦截 / CDP 取响应体 + cookies         │  │    │
│  │  └───────────────────────┬───────────────────────────┘  │    │
│  │                          ▼                              │    │
│  │  ┌───────────────────────────────────────────────────┐  │    │
│  │  │  Node HTTP 通道（默认）：undici/fetch + UA/Referer │  │    │
│  │  │  失败自动切换 browser / auto 通道                   │  │    │
│  │  └───────────────────────┬───────────────────────────┘  │    │
│  │  ┌───────────────────────▼───────────────────────────┐  │    │
│  │  │  storage (pg) · scheduler · notifier · config     │  │    │
│  │  └───────────────────────────────────────────────────┘  │    │
│  └───────────────────────────┬──────────────────────────────┘    │
│                              │  ipcMain ◄─contextBridge(preload) │
│  ┌───────────────────────────▼──────────────────────────────┐    │
│  │   渲染进程 Vue3 + TS + ECharts（页面；不参与爬取）        │    │
│  └──────────────────────────────────────────────────────────┘    │
└──────────────┬───────────────────────────────────────────────────┘
               │
    ┌──────────▼───────────┐   ┌──────────────────┐
    │      PostgreSQL      │   │  DeepSeek API    │
    └──────────────────────┘   └──────────────────┘
               │
    ┌──────────▼───────────────────────────────────────┐
    │  数据源：东财(f10/pingzhongdata/jjcc/push2his)    │
    │  蛋卷 djapi · 财联社 cls.cn · (估值/兜底)雪球       │
    └──────────────────────────────────────────────────┘
```

### 7.1 crawler（基金与个股数据抓取，主进程）

- **统一取数入口**：`crawler/fetch.go` 式封装为 `httpClient.ts`（Node 通道，默认）+ `pageFetcher.ts`（browser 通道兜底），`config.fetch.channel` 按源可配。
- **启动补全**：对每个自选基金并行拉取 `pingzhongdata`（名称/全量历史净值/持仓股代码）+ 蛋卷 `djapi`（经理/成立日）+ `jjcc`（季度重仓股权重）。
- **每日增量**：盘后每 10 分钟检查 `lsjz pageIndex=1`，直到出现当日净值 → 写入 `fund_nav_daily` → 停。
- **盘中估值**：交易时段按配置间隔（默认 30s）取跟踪指数/ETF 实时涨跌（T1）或页面估值（T2），写入 `fund_estimate`。
- **重仓股行情**：盘中 `push2` 实时；收盘后 `push2his` 日 K 增量写 `stock_daily`。
- 解析失败/超时：日志 + 指数退避 + 通道切换。

### 7.2 news（只读 `ai_fund.raw_news`）

- **不抓取**：财联社采集/签名/LLM 增强由另一套 24h 程序完成（写入 `ai_fund.raw_news`）。
- 本应用 `news/reader.ts` 只读查询：最新 N 条（时间线）、按 `llm_tags`/`tags`/标题关键词过滤（AI 分析选材）。
- 界面按标签筛选 + LLM 摘要与情绪色标（`summary`/`sentiment`/`llm_tags` 字段直接用）。

### 7.3 analyzer（DeepSeek 建议）

- **触发**：每个交易日收盘后一次（默认 15:30）；可配置盘中定时（cron 式分钟字段）。
- **输入**：基金近 30/60/120 日净值 + 今日涨跌 + 重仓股近 10 日涨跌 + 相关新闻（按 llm_tags 前 10 条）+ 用户持仓（份额/成本/浮动盈亏）。
- **Prompt**：说明数据为场外基金日净值与重仓股行情，仅基于数据判断，输出固定 JSON：
  ```json
  { "action": "add|reduce|hold", "confidence": 0~100, "reason": "中文理由（150字内）" }
  ```
- 结果写 `ds_advice` 保留 `response_raw`；`action != hold` → 桌面通知 + 站内建议列表。
- 失败处理：超时/限流记日志本次跳过，不重试轰炸。

### 7.4 前端页面（渲染进程，经 preload IPC 取数）

| 页面 | 内容 |
|---|---|
| 我的基金 | 自选基金卡片（名称/净值/今日涨跌/盘中估值），添加/停用；点击进详情 |
| 基金详情 | 净值走势图（近 1 月/3 月/1 年/全部）、盘中估值叠加、重仓股列表（权重+近 10 日表现）、AI 建议区 |
| 新闻资讯 | `ai_fund.raw_news` 快讯流（时间线），按标签筛选，LLM 摘要与情绪色标 |
| 我的持仓 | 按基金录入买卖（份额/净值/费率）、收益汇总，AI 建议联动 |
| 设置 | PostgreSQL DSN、DeepSeek Key/baseUrl/model、抓取开关与频率、通道选择（写回 config.json） |

### 7.5 配置 config

- 应用专属目录 `config.json`（`app.getPath('userData')/config.json`），首次运行自动生成模板：

```json
{
  "pg": { "user": "...", "password": "...", "host": "...", "port": 5432, "db": "fund_monitor", "sslmode": "prefer" },
  "aiFund": { "user": "", "password": "", "host": "", "port": 0, "db": "ai_fund", "sslmode": "" },
  "deepseek": { "apiKey": "", "baseUrl": "https://api.deepseek.com", "model": "deepseek-chat" },
  "fetcher": { "navCheckMinutes": 10, "holdingsRefreshDays": 7, "estimateIntervalSeconds": 30 },
  "analyzer": { "minutes": "35" },
  "fetch": { "channel": "node" },      // node / browser / auto（默认 node 直连，失败切 browser）
  "funds": []                           // 初始为空，用户在"我的基金"页添加
}
```

> `aiFund` 为新闻只读连接：通常与 `pg` 同实例同凭证、仅 db 名不同（默认 `ai_fund`）。字段留空时自动继承 `pg` 对应值；也支持 `.env` 的 `AI_FUND_URL` 或 `AI_FUND_*` 覆盖。

- 开发回退：项目根 `.env`，首次运行迁移到 `config.json`。
- 启动校验 DB 连通性 + 自动建表；Key 缺失跳过 AI 分析。

---

## 八、并发与一致性

| 场景 | 方案 |
|---|---|
| 多客户端同时写同一基金同日净值 | `UNIQUE(fund_code, trade_date)` + `ON CONFLICT DO NOTHING` |
| 当日净值滚动更新 | `ON CONFLICT DO UPDATE` |
| 新闻重复 | 不写入：本应用只读 `ai_fund.raw_news`，去重由采集程序负责 |
| 重仓股/个股行情/估值重复 | `UNIQUE` 键 + `DO NOTHING` |
| 时间键来源 | 一律用数据源自带日期（FSRQ / ctime / trade_date），不用本机时间 |
| 表结构并发变更 | 启动 `CREATE TABLE IF NOT EXISTS` 幂等，v0 不引入迁移工具 |

---

## 九、项目目录结构（规划）

```
fund_monitor/
├── 计划书.md
├── package.json               # electron + vue + 依赖
├── electron.vite.config.ts
├── electron-builder.yml       # NSIS 打包配置
├── src/
│   ├── main/                  # 主进程（Node，爬虫与核心逻辑）
│   │   ├── index.ts           # 窗口/生命周期/单实例锁
│   │   ├── ipc.ts             # ipcMain 处理器（渲染进程调用入口）
│   │   ├── config.ts          # config.json 读写/.env 回退
│   │   ├── crawler/
│   │   │   ├── httpClient.ts  # Node 通道：undici/fetch + UA/Referer/退避
│   │   │   ├── pageFetcher.ts # browser 通道：隐藏窗口 + executeJavaScript/webRequest/CDP/cookies
│   │   │   ├── eastmoney.ts   # lsjz / pingzhongdata / jjcc / push2his / push2
│   │   │   ├── danjuan.ts     # 蛋卷详情
│   │   │   └── estimate.ts    # 盘中估值 T1(跟踪指数) / T2(页面)
│   │   ├── news/              # 只读 ai_fund.raw_news（reader.ts；采集由另一程序负责）
│   │   ├── analyzer/          # DeepSeek 建议、prompt、结果解析
│   │   ├── position/          # 多基金持仓计算
│   │   ├── storage/           # pg 连接、建表、各表查询
│   │   ├── notifier.ts        # Electron Notification
│   │   └── scheduler.ts       # 定时任务（净值盘后/新闻轮询/估值/AI）
│   ├── preload/
│   │   └── index.ts           # contextBridge 安全暴露 API
│   └── renderer/              # Vue3 + TS + ECharts + Naive UI
│       ├── src/               # 组件（Dashboard/FundDetail/News/Portfolio/Settings）
│       └── index.html
├── tests/                     # vitest 单测 + 爬虫契约测试（可选 Playwright e2e）
└── build/                     # 图标、安装包资源
```

---

## 十、实施里程碑

- [x] **M1 项目初始化**：electron-vite 脚手架（Vue3 + TS），空窗口跑通，preload + IPC 最小链路，electron-builder 出开发包。
- [x] **M2 数据底座**：`config.json`/`.env` 配置 + pg 连接 + 自动建表 + 命令行验证（主进程脚本 `-check`）。
- [x] **M3 双通道爬虫 + 基金数据闭环**：`httpClient`（Node 直连 lsjz/pingzhongdata/蛋卷/jjcc）+ `pageFetcher`（隐藏窗口兜底）；净值全量补种 + 每日增量 → 幂等入库；脚本 `-fund 017193` 拉一次并打印。已实测：017193（764 条）与 161005（5060 条）全量补种、增量幂等、持仓 2 期入库、隐藏窗口抓页。建独立库 `fund_monitor`（`scripts/ensure-db.js`），避免与 gold_monitor 共用 `ds_advice` 冲突。
- [x] **M4 个股行情 + 盘中估值(T1)**：push2/push2his 入库；跟踪指数实时估算入库（INDEX_RULES 扩展至 17 条）。新闻**不抓取**——读取 `ai_fund` 库 `raw_news` 表（另一套 24h 程序采集 + LLM 增强），`--news` 变读取验证 CLI，`news:recent` IPC 供界面用，自有 `raw_news` 表从 schema 移除。
- [x] **M5 前端可视化**：我的基金（卡片列表 + 添加/停用 + 盘中估值）/ 基金详情（净值 ECharts 走势归一化 + 重仓股近10日表现 + AI 建议区）/ 新闻流（ai_fund.raw_news 时间线 + 关键词筛选 + 情绪/主题标签色标）/ 设置（数据库只读展示 + DeepSeek + 抓取频率写回 config.json）。vue-router + naive-ui + echarts，主进程新增聚合查询层 `storage/queries.ts` 与 `--screenshot` 开发辅助命令；四页面已截图验证。
- [x] **M6 AI 分析 + 通知**：`analyzer/analyze.ts`（取数：120日净值 + 重仓股近10日 + ai_fund 相关新闻 → DeepSeek 固定 JSON → 解析校验 → 写 `ds_advice` 保留 response_raw）、`notifier.ts`（Electron Notification，action != hold 弹窗）、CLI `--analyze <code>` / `--analyze-all`、IPC `advice:analyze`、详情页「立即分析」按钮。parseAdvice 单测 6 用例全过；未配置 Key 时优雅跳过（退出码 2）。完整 LLM 实测待配置 DeepSeek API Key 后验证。
- [x] **M7 持仓管理**：`position/position.ts`（移动加权平均：买入更新成本含费、卖出计已实现盈亏、超卖截断）、IPC `position:list/detail/addTrade/deleteTrade/profile`、前端「我的持仓」页（持仓汇总 + 浮动盈亏/收益率 + 交易流水增删 + 费率设置）。算法单测 4 用例全过；110020 端到端验证：300 份移动加权成本 1.8783、市值 571.71、浮动盈亏 +8.21 均精确。
- [x] **M8 打磨**：README 重写；**多源降级**——httpClient 改 Node 原生 https（Electron fetch 走代理栈对部分行情域名连接失败），push2/push2his 失败自动降级腾讯 qt.gtimg 实时 + web.ifzq 日 K（东财行情偶发 IP 风控实测触发）；**估值 T2 改主题 ETF 映射**（页面估值接口 fundgz 已下线，用同主题 ETF 实时价替代，source='theme_etf'，已实测 017193→有色ETF 1.12% 入库）；前端 T1/T2 标签展示；NSIS 打包 `dist/fund_monitor-0.1.0-setup.exe`（110MB）已生成、unpacked 版 `--check` 实测通过。

---

## 十一、风险与待确认事项

| # | 事项 | 说明 / 对策 |
|---|---|---|
| 1 | `fundgz` 盘中估值接口已失效（404） | 确认彻底失效；估值走 §4.4 分层（T1 跟踪指数 → T2 页面） |
| 2 | `fundmobapi`（移动端估值/持仓）返回 61136 | 带完整参数仍失败，不可靠；持仓用 `FundArchivesDatas`，估值用 §4.4 |
| 3 | 东财/财联社接口字段变化、限流 | 解析失败只记日志不崩溃；合理频率 + 双通道自动切换 |
| 4 | 净值 T 日 20:00+ 公布 | 盘后 10 分钟轮询直至当日数据出现；盘中参考估值与重仓股 |
| 5 | 财联社签名算法可能变化 | 签名集中在 `news/sign.ts` 单文件；失败静默跳过 |
| 6 | 页面抓取依赖 DOM/接口稳定（T2 估值、雪球） | 隐藏窗口优先"嗅探页面估值接口"而非 DOM；接口优先策略，DOM 仅兜底 |
| 7 | 跟踪指数识别失败（基金名→指数代码） | 名称关键词匹配；匹配不到降级 T2/T3 |
| 8 | DeepSeek 费用/限流 | 默认每交易日收盘后 1 次；Key 未配置自动跳过；设置页可调 |
| 9 | Electron 体积 ~150MB / 内存占用 | 个人工具可接受；打包 NSIS 压缩；渲染进程不参与爬取降低占用 |
| 10 | 首装 Electron 二进制下载 | electron-builder 需网络；本机已有代理环境即可 |
| 11 | 多基金补种量 | 单基金历史净值约 1~2k 条，分页 + `ON CONFLICT DO NOTHING`，幂等可重跑 |

---

## 十二、下一步

1. 范围与路线已确认：**Electron**、盘中估值必做（§4.4）、无默认基金清单（`funds` 初始为空）。
2. 通过后从 **M1（electron-vite 脚手架 + IPC 最小链路）** 开始实施，按里程碑推进。
3. 实施 M3 先落地双通道爬虫 + 净值闭环，M4 落 T1 估值，M8 补 T2 页面估值与打包。

---

*参考：gold_monitor/计划书.md（产品形态与表结构蓝本）；ai-fund-recommend/design.md（功能清单与数据源调研起点）；Electron 官方文档（main/renderer/preload、session.webRequest、webContents.debugger）。*
